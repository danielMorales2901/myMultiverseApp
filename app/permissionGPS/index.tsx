//index.tsx

import { LocationView } from "@/components/permissionGPS/locationView";

export default function LocationScreen() {
  return (
    /* Archivo para mostrar la ubicación actual del dispositivo en un mapa */
    <LocationView />
  );
}